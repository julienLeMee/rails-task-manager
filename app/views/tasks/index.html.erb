<h1>My Tasks</h1>
<%= link_to 'Add a task', new_task_path , class: "btn btn-primary"%>

<ul class="m-3">
  <% @tasks.each do |task| %>
    <li><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>
