<h2 class="m-3"><%= @task.title %></h2>
<p>- <%= @task.details %></p>
<p><%= @task.completed %></p>

<div><%= link_to 'Edit task', edit_task_path, class: "btn btn-success"%> | <%= link_to "Delete", task_path(@task), class: "btn btn-success",
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}%></div>

<%= link_to 'Back Home', tasks_path, class: "m-3 btn btn-primary"%>
